stages:
  - schedule

schedule_pipeline:
  stage: manual
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: always
    - allow_failure: true
  only:
    - schedules
  script:
    - bash env_cleanup_schedule.sh ${CI_JOB_TOKEN}
  tags:
    - sts-k8s-xl-runner
