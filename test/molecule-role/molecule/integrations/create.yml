---
- name: Create
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - include_role:
        name: ../common/create
      vars:
        security_group_name: molecule_integrations
