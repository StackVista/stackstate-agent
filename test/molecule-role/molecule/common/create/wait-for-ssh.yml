---
- name: Wait for SSH
  wait_for:
    port: "22"
    host: "{{ item.address }}"
    search_regex: SSH
    delay: 10
    timeout: 320
  with_items: "{{ lookup('file', molecule_instance_config) | molecule_from_yaml }}"
# {{ ssh_port }}
