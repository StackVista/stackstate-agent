---
provisioner:
  name: ansible
  options:
    'vault-id': "${MOLECULE_SCENARIO_NAME}@${CI_PROJECT_DIR}/test/molecule-role/molecule/vault-password.py" "${CI_PROJECT_DIR}/.cache/molecule/molecule-role/${MOLECULE_SCENARIO_NAME}/ssh_key"
  playbooks:
    create: playbook.setup.create.yml
    prepare: playbook.setup.prepare.yml
    destroy: playbook.setup.destroy.yml
  lint:
    name: ansible-lint
  inventory:
    links:
      group_vars: group_vars/
